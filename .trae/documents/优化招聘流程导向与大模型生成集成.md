## 目标与效果
- 优化“面向招聘方”的流程导向，突出每一步的标题与排版（醒目颜色/留白/分隔线），提升操作引导。
- 调整步骤结构与交互：
  1. 步骤1优先“上传JD文件”，自然语言输入放在次位并提供示例；
  2. 结构化JD扩展为“硬性要求/软性要求/实体特征”分组展示；
  3. 步骤2的简历来源支持“指定目录路径”输入；
  4. 步骤3仅输出“评分最高的前10个”；
  5. 步骤4做“综合匹配输出”：点击候选人查看详情（雷达+饼图），细列各特征匹配分，区分软硬匹配；
  6. 移除原“步骤5”，并将“面试题生成/优势风险分析”合并进步骤4，使用阿里通义千问或开源轻量模型生成。

## 前端改造（Streamlit）
- 高亮排版：
  - 为每个步骤标题使用加粗/颜色条与分隔线；在“步骤卡片”顶部加入醒目说明。
  - 改动位置：`scripts/06_visualization.py:page_hr_wizard`（约1209起）。
- 步骤1（岗位需求）：
  - 将“上传JD文件（txt/md/docx/pdf）”置于自然语言输入之前；自然语言输入下方增加“示例JD文本”，一键填充。
  - 生成结构化JD后，按分组卡片展示：硬性（学历/年限/职位/薪资/雇佣类型/地点/行业）、软性（技能/框架/工具/语言/软技能）、实体特征（关键词/证书）。
  - 扩展后的表格支持 Excel/CSV 导出。
- 步骤2（简历来源/在线采集）：
  - “简历来源”提供目录路径输入（默认 `data/raw_resumes`），支持解析常见格式；
  - 在线采集保持 Tab 二选一；解析后写入 `st.session_state["hr_resumes"]`；岗位池写入 `st.session_state["hr_jobs_pool"]`。
- 步骤3（匹配与评分）：
  - 执行匹配后只保留前10名，写入 `st.session_state["hr_match_results"]`。
- 步骤4（综合匹配输出）：
  - 上方展示 Top10 的综合雷达对比（8维：技能/学历/年限/职位/关键词/证书/语言/格式）；
  - 下方提供候选人选择器，点击后展示单人详情卡片：
    - 雷达图+能力占比饼图
    - 细项评分列表（每维分数与命中细节）
    - 面试题（3条，基于该候选与JD）
    - 综合分析（优势/风险），基于大模型生成。
  - 保持“导出报告”按钮（TXT/HTML/Excel）。

## 后端与大模型集成
- 扩展结构化JD：
  - 已在 `modules/llm_utils.py:generate_structured_jd` 输出更多字段（硬性/软性/实体特征），继续按分组在前端展示。
- 统一大模型生成接口：
  - 保留 `/interview_questions` 与 `/evaluation_report` 路由（`scripts/05_api.py`），新增“LLM_PROVIDER”与“API KEY”配置；
  - 新增 `modules/llm_providers.py` 提供统一适配层：
    - Provider: `qwen`（阿里云通义千问，`QWEN_API_KEY`、`QWEN_MODEL`）；
    - Provider: `local`（开源轻量模型，如 `Qwen2.5-0.5B-Instruct` 或 `phi-3-mini`，本地推理，低资源）
  - 当 `LLM_PROVIDER=qwen` 且密钥有效时调用通义千问 API；否则回退到本地轻量模型。
- 安全与配置：
  - 所有密钥通过环境变量注入，不写入仓库；在“高级设置”内仅显示说明与状态，不展示密钥内容。

## 交互与数据流
- 步骤1输出结构化JD对象：用于后续匹配与题目生成的上下文；
- 步骤2解析简历/采集岗位：分别存入 `hr_resumes` 与 `hr_jobs_pool`；
- 步骤3调用 `/match`，向量检索/融合排序，保留Top10；
- 步骤4：
  - 综合雷达对比使用Top10的细项分；
  - 单人详情调用 `/interview_questions` 与 `/evaluation_report` 并展示结果（按 Provider）。

## 验证与交付
- 本地验证：
  - 使用默认 `data/raw_resumes` 与 `data/raw_jobs` 目录进行流程；
  - 验证：
    - 步骤1能生成并分组显示结构化JD，导出成功；
    - 步骤2能解析目录并在线采集合并岗位池；
    - 步骤3仅显示Top10，并可导出；
    - 步骤4雷达对比 + 单人卡片完整显示，题目与分析生成正常。
- 可选 Provider 测试：
  - 设置 `LLM_PROVIDER=qwen` 与 `QWEN_API_KEY` 后调用通义千问；
  - 未配置时回退到本地轻量模型，确保无外网也能体验。

## 文件改动概览
- `scripts/06_visualization.py`：
  - page_hr_wizard：重排步骤1；扩展 JD 展示分组；步骤2目录输入增强；步骤3保留Top10；步骤4综合对比+单人卡片；移除原步骤5合并到步骤4。
  - 复用 `_render_candidate_insights` 渲染详情卡片（雷达/饼图/题目/分析）。
- `modules/llm_utils.py`：结构化JD扩展字段（已完成基础扩展）。
- `scripts/05_api.py`：LLM接口保持不变，新增 Provider 选择与密钥加载（通过适配层）。
- `modules/llm_providers.py`：新增，适配通义千问与本地模型实现。

## 时间与风险
- UI排版与交互：低风险，可迭代微调；
- LLM对接：需正确配置密钥与模型名，提供回退方案避免阻塞；
- 数据目录解析：保留默认与用户自定义，做好不存在提示与错误处理。

请确认以上方案，我将开始实现：前端排版与步骤重构、LLM Provider 适配与配置、最终联调与验证。