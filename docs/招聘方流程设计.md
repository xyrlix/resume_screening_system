## ✅ 优化后的简历匹配全流程

> **参考文档**：[架构设计文档](架构设计.md) - 包含系统整体架构和端到端业务流程

```mermaid
graph LR
%% ============ 第一阶段：输入与存储 ============
A[招聘方选择JD/简历来源] --> B[简历来源]
A --> C[JD来源]

B --> B1[上传文件: PDF/Word/图片/表格?TXT/MD等]
B --> B2[粘贴纯文本]
B --> B3[对接招聘平台API: BOSS/猎聘/LinkedIn等]

C --> C1[上传JD文件: Word/PDF等]
C --> C2[输入纯文本JD]

B1 --> B4[统一存储: /uploads/resumes/<id>.<ext>]
B2 --> B4
B3 --> B4

C1 --> C5[统一存储: /uploads/jds/<id>.<ext>]
C2 --> C5

%% ============ 第二阶段：多模态解析 ============
B4 --> D[简历多模态解析引擎]
C5 --> E[JD解析引擎]

D --> D1[PDF/Word: PyPDF2 / python-docx]
D --> D2[图片/扫描件: PaddleOCR + Layout Analysis]
D --> D3[表格简历: camelot-py / tabula-py]
D1 --> D4[原始文本输出]
D2 --> D4
D3 --> D4

E --> E1[JD文本提取（通常为纯文本）]
E1 --> E2[结构化解析：岗位名称、部门、汇报线、硬性要求等]

D4 --> F[文本标准化清洗]
F --> F1[去除乱码/广告/页眉页脚]
F --> F2[统一日期/数字/单位格式]
F --> F3[段落结构识别：教育/工作/项目等区块分割]

%% ============ 第三阶段：特征工程 ============
F3 --> G[结构化实体抽取]
G --> G1[基础信息：姓名、电话、邮箱、地点]
G --> G2[教育背景：学校、专业、学历、时间]
G --> G3[工作经历：公司、职位、时间、职责]
G --> G4[项目经验：名称、角色、技术栈、成果]
G --> G5[技能证书：语言、工具、认证]
G --> G6[衍生特征：总工龄、跳槽频率、行业垂直度]

F3 --> H[语义向量化]
H --> H1[全文向量：BGE-M3 dense <1024维>]
H --> H2[分段向量：工作/项目/技能分别编码]
H --> H3[关键词分布：TF-IDF / LDA]
H --> H4[软技能信号：NLP规则+LLM抽取]
H --> H5[职业轨迹序列：用于LSTM建模（可选）]

E2 --> I[JD结构化特征]
I --> I1[硬性条件：学历≥本科、3年经验等]
I --> I2[JD语义向量：BGE-M3 编码]
I --> I3[JD关键词权重]

%% ============ 第四阶段：三级漏斗匹配 ============
G & H & I --> J[三级漏斗匹配引擎]

J --> J1[一级：硬性规则过滤]
J1 --> J1a[规则引擎加载JD硬性条件]
J1 --> J1b[结构化字段匹配（学历/年限/城市等）]
J1 --> J1c[输出：通过初筛简历池]

J1c --> J2[二级：AI语义匹配打分]
J2 --> J2a[BGE-M3 向量相似度计算（dense + sparse）]
J2 --> J2b[加权融合：工作<0.5> + 项目<0.3> + 技能<0.2>]
J2 --> J2c[阈值过滤 or Top-K 排序]
J2 --> J2d[输出：高匹配度简历列表]

J2d --> J3[三级：大模型深度补筛（可选）]
J3 --> J3a[输入：简历原文 + JD + 二级得分]
J3 --> J3b[LLM Prompt：分析稳定性、成长性、真实性、文化匹配]
J3 --> J3c[输出：结构化风险标签 + 综合评语]

%% ============ 第五阶段：结果输出 ============
J1c & J2d & J3c --> K[结果聚合与呈现]

K --> K1[匹配评分排序（0~100）]
K --> K2[人才画像：技能图谱、职业路径]
K --> K3[预测模型输出：
    - 离职风险
    - 薪资区间
    - 稳定性指数
    - 项目深度评分]
K --> K4[综合报告：优势 + 风险点 + 面试建议]

K1 --> L[前端展示：TOP N 简历卡片]
K2 --> L
K3 --> L
K4 --> L
```

---

## 🔧 关键调整说明（为什么这样改？）

### 1. **拆分“解析”与“清洗”阶段**

- 原流程中 `B6（文本清洗）` 同时做降噪、缺失、异常处理，但这些属于**数据质量治理**，应在**结构化抽取前完成**。
- 新增 **段落结构识别**（如区分“工作经历”和“项目经验”），这是后续精准抽取的前提。

### 2. **JD 也需结构化解析**

- 原流程只对简历做特征提取，但 **JD 的硬性条件必须结构化**（如“3 年以上经验”→ `min_exp=3`），否则无法做一级规则过滤。
- JD 也需要向量化，用于二级语义匹配。

### 3. **三级漏斗顺序必须严格线性**

- 原图中 `D --> D2`, `D --> D1`, `D --> D3` 是并行的，但实际应为 **串行漏斗**：
  > 一级过滤 → 二级打分 → 三级补筛  
  > 否则会浪费算力（对明显不符者做语义计算）。

### 4. **三级补筛应为“可选”且轻量**

- 大模型（LLM）推理成本高，**不应作为必经环节**。
- 建议仅对二级 Top 50 或边界案例（如得分 0.55~0.65）启用 LLM 补筛。

### 5. **特征工程模块职责单一化**

- 将“实体特征”“语义特征”“格式特征”合并为两个主干：
  - **结构化实体抽取**（用于规则 + 衍生特征）
  - **语义向量化**（用于相似度计算）
- “格式特征”（如排版工整度）在真实场景中价值有限，建议移至**可选扩展模块**。

### 6. **结果输出需聚合而非分散连接**

- 原图中每个子节点都连到 `E`，导致结果碎片化。
- 新流程通过 **`K[结果聚合]`** 统一整合评分、画像、预测、评语，再输出到前端。

### 7. **增加“可配置性”提示**

- 规则阈值、权重比例、是否启用 LLM 补筛等，应设计为 **可配置参数**，而非硬编码。

### 8. **实现对齐说明**

- 已实现多模态解析与统一落地：`/uploads/resumes`、`/uploads/jds`
- 已实现分段向量（经验/技能/教育）加权融合与参数配置入口
- 已实现 LLM 补筛可选及边界区间触发（默认关闭可选开关）
- 已实现线上导入认证（用户名/密码或 Cookie）与来源追踪

---

## 📌 最佳实践建议

| 模块         | 建议                                                                                                                          |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| **OCR 解析** | 使用 PaddleOCR + LayoutParser 提升表格/多栏简历准确率                                                                         |
| **实体抽取** | 用 spaCy + 自定义规则 + 轻量 LLM（如 Qwen-1.8B-Chat）联合抽取                                                                 |
| **向量模型** | **BGE-M3 dense 模式** 足够，sparse/multi-vector 仅在高精度场景启用                                                            |
| **LLM 补筛** | 使用 **Qwen-Max / Kimi / DeepSeek-Coder**，Prompt 示例：<br>`你是一名资深HR，请从稳定性、成长性、真实性三方面评估该候选人...` |
| **部署架构** | 解析 → 特征 → 匹配 分阶段异步处理，避免阻塞                                                                                   |

---

## 三级漏斗 LLM 评分 Prompt 示例:

```
你是一名资深HR专家，正在评估一位候选人是否适合以下岗位：

【岗位要求】
{{JD_SUMMARY}}

【候选人简历原文】
{{RESUME_TEXT}}

请严格基于简历内容，从以下四个维度进行客观分析，并按指定JSON格式输出结果。不要编造信息，若某项无法判断，请填"无法判断"。

分析维度：
1. **职业稳定性**：是否频繁跳槽？单段工作时长是否过短（<1年）？是否有空窗期？
2. **成长轨迹**：职位/职责是否持续进阶？是否有从执行到主导的转变？
3. **内容真实性**：时间线是否冲突？成果描述是否模糊（如“大幅提升”无数据）？
4. **文化匹配潜力**：是否体现自驱力、协作意识、结果导向等软技能？

输出格式（必须为合法JSON，不要任何额外文本）：
{
  "stability": {
    "score": 0~5分,
    "evidence": "具体依据，引用简历原文",
    "risk_flag": true/false
  },
  "growth": {
    "score": 0~5分,
    "evidence": "具体依据",
    "positive_signal": true/false
  },
  "authenticity": {
    "score": 0~5分,
    "issues": ["问题1", "问题2"] 或 [],
    "verifiable": true/false
  },
  "culture_fit": {
    "score": 0~5分,
    "soft_skills": ["自驱力", "沟通能力", ...] 或 [],
    "potential": "高/中/低"
  },
  "overall_recommendation": "推荐/谨慎推荐/不推荐",
  "interview_focus": ["建议面试重点考察的问题1", "问题2"]
}
```
